export interface TokenCommandOptions {
  env?: string;
  namespace?: string;
  token?: string;
  logLevel?: string;
  verbose?: boolean;
  tokenUrl?: string;
  userinfoUrl?: string;
  clientId?: string;
}
