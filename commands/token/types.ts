export interface TokenCommandOptions {
  env?: string;
  profile?: string;
  token?: string;
  logLevel?: string;
  verbose?: boolean;
  tokenUrl?: string;
  userinfoUrl?: string;
  clientId?: string;
}
